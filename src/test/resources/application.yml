# Spring Config
spring:
    profiles:
        active: accelerate,accelerate.logging
    
    application:
        name: Accelerate
        index: 1

# Accelerate Config
accelerate:
    utils:
        logging:
            audit: true
            metrics: true
            debug: true
        staticListener:
            scanBasePackages: accelerate
